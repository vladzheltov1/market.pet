import { FilterCondition } from "../types/filter";
import { Tables } from "../types/tables";

// Utils

const select = (table: keyof typeof Tables, conditions: FilterCondition) => { }

export {
    select
};
